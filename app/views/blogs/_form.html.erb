  <%= form_with(model: @blog, local: true) do |form| %>
    <div class="form-group">
      <%= form.text_field :title, placeholder: 'ブログタイトル' %>
    </div>
    <div class="form-group">
      <%= form.select :category, ['最近のこと', 'ごはんレシピ'], { include_blank: 'カテゴリを選択してください' } %>
    </div>
    <div class="form-group">
      <%= form.text_field :content, id: :article_text, class: "form-control" %>
      <trix-editor input="article_text"></trix-editor>
    </div>
    <div class="form-group">
      <%= form.label :image %>
      <%= form.file_field :image %>
    </div>
  <%= form.submit class: 'btn btn-primary' %>
  <% end %>
